ktor {
    deployment {
        port = 8080
        host = localhost
    }
    application {
        modules = [ delivery.ApplicationKt.module ]
    }
}

jdbc{
        url =  "jdbc:postgresql://localhost:5432/shop"
        username = "postgres"
        password = "admin"
}
